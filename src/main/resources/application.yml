spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/test
    username: root
    password: Ymk0910!
  thymeleaf:
    cache: false  # 关闭缓存，调试用


mybatis-plus:
  global-config:
    db-config:
      id-type: auto  # 主键自增
  mapper-locations: classpath:mapper/*Mapper.xml  # mapper文件位置
  type-aliases-package: org.kkhimself.java_base.model       # 实体类位置